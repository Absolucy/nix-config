{ pkgs, ... }:
{
  programs.ssh = {
    enable = true;
    matchBlocks = {
      "github.com" = {
        user = "git";
        identityFile = [
          "~/.ssh/github.pub"
        ];
        cat = true;
      };
    };
  };
}
